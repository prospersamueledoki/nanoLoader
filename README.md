# nanoLoader
NanoLoader is a lightweight CSS &amp; JS library that provides a collection of smooth, customizable loading animations. It works on mobile and desktop, supports 15+ color options, and lets you resize or restyle loaders via HTML classes or JavaScript.
